[project]
name = "ai-travel-planner"
version = "0.1.0"
description = "A Streamlit-based travel planning assistant"
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["ai_travel_planner"]
